# Maintainer: Murat AKPINAR <akpinarmurat@protonmail.com>

pkgname=ratodo
pkgver=0.5.0
pkgrel=1
pkgdesc='A todo TUI, built with ratatui — one Markdown file, no cloud, no account'
arch=('x86_64' 'aarch64')
url='https://github.com/murat-akpinar/ratodo'
license=('GPL-3.0-only')
depends=('gcc-libs' 'glibc')
makedepends=('cargo')
source=("$pkgname-$pkgver.tar.gz::$url/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('5efb7dcf4e43a076814718df5c2d86595a8477d3ba2b71d73741d510427c5a8d')

prepare() {
	cd "$pkgname-$pkgver"
	export RUSTUP_TOOLCHAIN=stable
	cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
	cd "$pkgname-$pkgver"
	export RUSTUP_TOOLCHAIN=stable
	export CARGO_TARGET_DIR=target
	cargo build --frozen --release --all-features
}

check() {
	cd "$pkgname-$pkgver"
	export RUSTUP_TOOLCHAIN=stable
	cargo test --frozen --all-features
}

package() {
	cd "$pkgname-$pkgver"
	install -Dm0755 -t "$pkgdir/usr/bin/" target/release/ratodo

	# Hand-written and shipped rather than generated at install time.
	install -Dm0644 completions/ratodo.bash "$pkgdir/usr/share/bash-completion/completions/$pkgname"
	install -Dm0644 completions/ratodo.zsh "$pkgdir/usr/share/zsh/site-functions/_$pkgname"
	install -Dm0644 completions/ratodo.fish "$pkgdir/usr/share/fish/vendor_completions.d/$pkgname.fish"

	install -Dm0644 -t "$pkgdir/usr/share/doc/$pkgname/" README.md CHANGELOG.md
	install -Dm0644 -t "$pkgdir/usr/share/licenses/$pkgname/" LICENSE
}
